# Feature Template

This template follows:

UI / Page / Widget -> BLoC -> Repository -> Provider -> API / Local Storage -> Mapper -> Model

Rename `sample` files to match the feature name and keep dependencies flowing in one direction.
